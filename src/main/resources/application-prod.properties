spring.application.name=siblhish-api

# Database Configuration
# Railway provides DATABASE_URL in format: postgresql://user:password@host:port/database
# Spring Boot needs JDBC format: jdbc:postgresql://host:port/database
# We use individual variables that Railway provides
spring.datasource.url=jdbc:postgresql://interchange.proxy.rlwy.net:32176/railway
spring.datasource.username=postgres
spring.datasource.password=HpzwhZSBpWjPpWcPRtZTYoktajSPmLvu
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=false

# API Configuration
server.servlet.context-path=/api/v1
# Railway définit automatiquement la variable PORT
server.port=8082

# Logging
logging.level.root=INFO
logging.level.ma.siblhish=DEBUG

